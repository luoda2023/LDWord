# Alavette Form V1.0.1 发布说明

> 构建状态：未签名 QA 候选包；正式分发前仍需组织代码签名。

Alavette Form V1.0.1 重点修复公文执行入口，并增强已有 DOCX 的公文套版能力。

## 本次更新

- 修复部分用户看不到“生成文档”按钮的问题。
- 公文入口自动区分“已有 DOCX 套版”和“资料包母版组装”。
- 已有 DOCX 根据所选文种套用内置公文母版；支持标题、主送机关、正文层级、附件、落款、日期和抄送等角色。
- 标题或正文识别不完整时继续生成并给出提示，仅在源文件确实无法读取时阻止。
- “函”使用独立信函母版，包括函版页边距、红色机关标志及双线、右对齐文号、正文和落款格式。

## 交付形式

- Windows 10/11 x64 `Setup.exe`（推荐）与便携 ZIP；安装或完整解压后运行 `Alavette-Form.exe`。
- 正式 ZIP 必须带数字签名、SHA-256、CycloneDX SBOM、文件清单、第三方许可和两份 PDF 用户文档。
- 文件名含 `UNSIGNED-QA` 的包只用于内部验证，不是正式发布物。

## 重要边界

- GUI 以 DOCX 为核心输入；CLI 当前仅接受 DOCX。
- 精确 Word 分页和部分复杂对象能力依赖本机 Microsoft Office；不可用时按界面提示降级。
- AI Provider 由用户自行配置；远程地址必须使用 HTTPS，本机回环服务可使用 HTTP。
- V1.0.x 不支持替代交付目标模板；配置后会在执行前明确阻断。
- 标书、技术文档和报告模式保留内部定义，但不作为 V1.0.x 当前可用模式承诺。

## 发布前仍需完成

- 在目标 Windows/Office/DPI 矩阵上完成人工验收。
- 使用组织代码签名证书重新生成并验证正式构建，然后创建 `v1.0.1` 标签。
