# Alavette Form V1.0 发布说明（候选稿）

> 状态：候选说明，尚未代表 V1.0 已正式发布。

Alavette Form 是 Windows 本地优先的场景化文档生产与规范化工作台。V1.0 面向通用、试卷、论文和公文四种当前可用模式，提供方案、模板、资料包、执行预检、DOCX 生成及可追溯交付能力。

## 交付形式

- Windows 10/11 x64 `Setup.exe`（推荐）与便携 ZIP；安装或完整解压后运行 `Alavette-Form.exe`。
- 正式 ZIP 必须带数字签名、SHA-256、CycloneDX SBOM、文件清单、第三方许可和两份 PDF 用户文档。
- 文件名含 `UNSIGNED-QA` 的包只用于内部验证，不是正式发布物。

## 重要边界

- GUI 以 DOCX 为核心输入；CLI 当前仅接受 DOCX。
- 精确 Word 分页和部分复杂对象能力依赖本机 Microsoft Office；不可用时按界面提示降级。
- AI Provider 由用户自行配置；远程地址必须使用 HTTPS，本机回环服务可使用 HTTP。
- V1.0 不支持替代交付目标模板；配置后会在执行前明确阻断。
- 标书、技术文档和报告模式保留内部定义，但不作为 V1.0 当前可用模式承诺。

## 发布前仍需完成

- 将候选改动归并为干净、可追踪的提交，并配置真实发布远端。
- 在目标 Windows/Office/DPI 矩阵上完成人工验收。
- 使用组织代码签名证书生成并验证正式构建，然后创建 `v1.0.0` 标签。
