# Security Policy

## 支持范围

正式发布后，维护者为最新的 `1.0.x` 版本提供安全修复。未签名构建或文件名包含 `UNSIGNED-QA` 的内部验证包不属于受支持发行版。

## 私密报告漏洞

发现安全漏洞请通过官网 https://dicad.cn 提供的渠道私密提交，不要通过公开 Issue 披露漏洞细节。

报告请尽量包含：

- 受影响版本和运行环境
- 可复现的最小步骤
- 预期影响与攻击前提
- 已彻底脱敏的样例或概念验证

请勿上传真实业务文档、个人资料、密钥、令牌或证书。若证据可能包含敏感信息，请先只描述影响和复现条件，等待维护者通过私密渠道确认接收方式。

## 凭据与隐私

- 不要把 API Key、证书、令牌或用户文档提交到仓库、Issue 或 Pull Request。
- 可选 AI Provider 的凭据由操作系统安全存储管理；分享日志和诊断材料前必须脱敏。
- 一旦怀疑凭据泄露，应立即在对应服务撤销并轮换；仅删除本地文件或 Git 提交并不能使凭据失效。
