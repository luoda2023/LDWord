# Security Policy

## 支持范围

正式发布后，维护者只为最新的 `1.0.x` 版本提供安全修复。尚未签名或文件名包含 `UNSIGNED-QA` 的构建不属于受支持发行版。

## 报告漏洞

请通过代码托管仓库的私密安全公告（Private vulnerability reporting / Security advisory）报告漏洞，不要在公开 Issue 中披露漏洞细节、密钥、个人资料或待处理文档。

在仓库尚未配置真实远端和私密报告入口前，安全报告渠道不完整，因此不应对外正式发布。维护者应在发布前启用该入口并在本文件补充可验证的仓库链接。

报告时请尽量包含受影响版本、复现步骤、影响判断和最小化的脱敏样例。请勿上传真实业务文档。

## 凭据与隐私

- 不要把 API Key、证书、令牌或用户文档提交到仓库或 Issue。
- AI Provider 凭据由操作系统安全存储管理；诊断材料应先脱敏。
- 怀疑凭据泄露时应立即在对应服务撤销并轮换，而不是只删除本地文件。
