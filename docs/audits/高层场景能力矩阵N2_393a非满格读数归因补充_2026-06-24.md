# 高层场景能力矩阵 N2.393a 非满格读数归因补充

## 目标

N2.393 已经把 release gate 中的 `count_profiles=14/15`、`delivery_families=14/15`、`maturity_l5_blocked=6/27` 纳入 `release_residual_ratio_ledger`。本补充继续收紧展示口径：ready 比例必须保留，边界不能伪装成 ready；同时所有非满格 ready 读数都必须给出 accounted 比例，说明剩余项已被边界或保留项归因。

## accounted 口径

| 读数 | ready 口径 | accounted 口径 | 归因 |
| --- | ---: | ---: | --- |
| CountProfile family | `14/15` | `15/15` | `ip_patent_documents` 是专业插件边界，仍保留 boundary |
| DeliveryPreset family | `14/15` | `15/15` | 专利交付 preset 归属专业 handoff 边界 |
| DeliveryPreset pack | `11/12` | `12/12` | `import_ai_boundary` 是 handoff/report 边界，不承诺 final-DOCX preset |
| Formula/output/watermark family | `14/15` | `15/15` | 专利输出归属场景输出边界，但不升级为 ready |

## 闭环要求

- `scene_count_profile_audit`、`scene_delivery_preset_audit`、`scene_formula_output_watermark_audit` 暴露 `accounted_*` 计数；
- `scene_matrix_dashboard` counts、卡片 detail、summary projection 展示 accounted 比例；
- `verify_scene_matrix_release_gate.py` 最终摘要展示 `count_profile_accounted=15/15`、`delivery_family_accounted=15/15`、`delivery_pack_accounted=12/12`、`fow_family_accounted=15/15`；
- `scene_release_residual_ratio_ledger_audit` 的 source evidence 检查 dashboard、summary、release gate 是否包含 accounted 口径；
- 测试必须同时锁住 ready 与 accounted，防止边界被误判为已实现。
