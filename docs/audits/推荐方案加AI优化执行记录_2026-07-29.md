# 推荐方案 + AI 优化执行记录

> 开始日期：2026-07-29  
> 当前状态：持续执行  
> 依据文档：`docs/audits/推荐方案加AI产品边界与非必要代码深度审计_2026-07-29.md`  
> 核心原则：先建立功能证据，再改变边界；工程审计能力可以迁出产品，但不得以减行数为由破坏 Form 核心闭环或 AI 路径 A/B/C。

## 1. 工作区约束

- 当前分支：`codex/code-structure-health-staging`
- 首次实施检查时 Git 状态条目：902
- 其中修改：419，删除：38，未跟踪：445
- 当前工作区包含大量已有重构，所有本轮改动均采用局部补丁，没有回退或覆盖其他修改。
- 本记录中的行数和模块数是当前工作区的时间点快照，不代表某个干净提交。

## 2. 阶段一结果

### 2.1 当前规模

| 指标 | 审计基线 | 当前实施结果 | 变化 |
|---|---:|---:|---:|
| 第一方打包有效行（真实 TOC、唯一源文件） | 269,306 | 184,664 | -84,642 |
| 主入口可达模块 | 672 | 522 | -150 |
| 主入口可达有效行 | 264,690 | 183,088 | -81,602 |
| 打包第一方模块（真实 TOC） | 712 | 559 | -153 |
| 打包的 `scene_matrix*` 模块 | 有 | 0 | 已移出 |
| 打包的 `scene_release_*` 模块 | 有 | 0 | 已移出 |

有效行采用“排除空行和纯注释、保留文档字符串”的同类口径。184,664 行来自 2026-07-29 最终 PyInstaller `PYZ-00.toc` 与 `Analysis-00.toc` 的**唯一第一方源文件**回算，不再是静态图估算。最终复核发现 PyInstaller 会同时以模块名和文件路径别名记录 `src/shared/ui/icons/__init__.py`、`catalog.py`；旧快照未完全规范化别名，记录值多算 200 行。当前数字按规范化绝对路径去重。TOC 相对主入口静态图额外包含 38 个文件、1,777 有效行，主要是生产附件处理、图片执行、AI form tools、共享 UI 白名单和包初始化模块。

阶段一的真实构建结果 225,631 行与审计预测 225,505 行接近；后续继续排除了矩阵集群外的工程证据、高级工作台、完整批量工作台、内部报告写入器、场景工程投影和专用附件工作台 handler。当前真实打包结果已进入 182,000～185,000 行长期推荐区间，后续不再以删除用户能力换取行数。

### 2.2 已完成：共享 UI 精确打包

修改：

- `scripts/windows/package_release.bat`
- `tests/test_release_shell.py`

处理内容：

1. 删除 `--collect-submodules src.shared.ui`；
2. 改为 16 个实际懒加载 UI 模块的显式 hidden-import 白名单；
3. 保留 `src.services.material_attachments` 当前收集规则，尚未在没有闭环测试前激进收窄；
4. 新增 AST 一致性测试，自动计算产品源码中 `from src.shared.ui import ...` 使用的导出，并要求与发布白名单完全一致。

该门禁保证：

- 未使用的共享 UI 不再进入产品；
- 新增懒加载 UI 使用却忘记打包时，测试会失败；
- 不需要把 `src.shared.ui.__init__` 改回全量即时导入。

### 2.3 已完成：场景矩阵与发布治理脱离产品静态图

修改：

- `src/ui/panels/scene_summary_projection.py`
- `tests/test_scene_panel_architecture.py`
- `tests/test_product_runtime_boundary.py`

处理内容：

1. 保留工程环境中的 `build_scene_matrix_drilldown_summary_items`；
2. 将其改为仅在显式工程调用时动态载入 `scene_matrix_drilldown`；
3. 产品 `ScenePanel` 静态导入图不再包含矩阵和发布治理；
4. 架构测试从只检查顶层 import，升级为检查整个 AST；
5. 新增从 `main.py` 计算本地模块可达图的门禁。

当前门禁要求：

- `src.config.scene_matrix*` 不得从产品入口静态可达；
- `src.config.scene_release_*` 不得从产品入口静态可达；
- `src.config.scene_boundary_maturity*` 不得从产品入口静态可达；
- 产品可达代码不得导入 `tests.*` 或 `scripts.*`；
- 产品入口模块预算不得超过 522。

工程 drilldown 仍可在源码环境调用，因此没有删除内部验收能力。

### 2.4 已完成：用户报告不再默认载入内部证据

修改：

- `src/report_writer.py`
- `tests/test_product_report_boundary.py`
- `tests/test_execution_diagnostics_reporting.py`
- `tests/test_scene_sample_fixture_regression.py`

处理内容：

1. `write_json_report` 和 `write_markdown_report` 新增 `include_internal_evidence=False`；
2. 正式生产调用保持默认值，不读取产品成熟度注册表或样本库 manifest；
3. 用户 JSON 不再出现 `scene_product_readiness`、`scene_sample_fixtures`、`parameter_ownership` 和 `control_contracts`；
4. 用户 Markdown 不再出现“产品成熟度证据”“样本库证据”“参数归属证据”和“控件契约证据”；
5. 工程测试显式传入 `include_internal_evidence=True` 后，仍能生成原有内部证据；
6. 产品成熟度、样本库、参数归属和控件契约注册表均改为显式工程模式下动态载入，避免报告模块自身形成静态打包边。

### 2.5 已完成：恢复 AI 结构预算

修改：

- `src/assistant/ui/creative_home.py`

处理内容：

- 将纯几何函数 `_grid_origin` 从 `AssistantCreativeHome` 类移到模块级；
- 类方法数从 37 恢复到预算 36；
- 网格坐标、绘制和 UI 行为不变；
- 没有删除 AI 会话、附件、Provider、计划、执行或恢复能力。

### 2.6 已完成：移除场景概览中的死诊断 UI

修改：

- `src/ui/panels/scene_panel.py`
- `src/ui/panels/scene_summary_projection.py`
- `tests/test_scene_panel_architecture.py`
- `tests/test_ui_copy_guardrails.py`

确认事实：

1. “诊断资料”卡片从未加入概览布局；
2. 父卡片被永久设为隐藏；
3. 只有测试通过私有属性直接展开和点击；
4. 正式用户无法到达“生成样本库、打开样本、请求单元筛选”等控件。

处理结果：

- 删除这段不可达 UI、按钮、列表、样式和事件处理；
- 场景首屏继续保留当前方案、执行预览、方案规则、风险/边界及跳转动作；
- 完整工程汇总、样本详情和请求单元投影仍保留在源码工程 API；
- 工程测试改为直接验证纯投影，不再通过客户面板伪装成产品功能。

### 2.7 已完成：生产执行不再附加开发样本库路径

修改：

- `src/services/production_runtime/delivery_reporting.py`
- `src/services/production_runtime/result_projection.py`
- `src/services/production_runtime/delivery_runtime.py`
- `tests/test_output_runtime_semantics.py`

处理内容：

- 停止在每次成功执行 payload 中查找并附加 `artifacts/scene_sample_fixtures/manifest.json`；
- 新执行结果不再包含 `scene_sample_manifest_paths`；
- 工作台适配器仍可读取旧 payload 中的同名字段，保留历史结果兼容；
- 样本库生成器和 manifest 读取函数仍保留给工程脚本与回归测试。

### 2.8 已完成：矩阵外工程注册表按需加载

从产品主入口静态图移出的模块包括：

- `src.config.scene_boundary_capability_matrix`
- `src.config.scene_product_readiness`
- `src.config.scene_sample_fixture_registry`
- `src.config.scene_request_cell_fixture_registry`
- `src.config.control_contract_registry`
- `src.config.scene_parameter_ownership`
- `src.config.scene_fixed_layout_profile_audit`
- `src.shared.engine.scene_sample_docx_builder`

这些模块在源码工程模式下仍可调用；产品默认路径不加载，也不再由 PyInstaller 静态分析自动带入。

### 2.9 已完成：复杂 DOCX 素材导入按格式加载

修改：

- `src/services/material_content/compiler.py`
- `tests/test_product_runtime_boundary.py`

处理内容：

- AI 路径 B 继续静态保留 Markdown 解析、图片资源校验、内容 IR、组合与 DOCX 渲染；
- `docx_normalizer`、`docx_importer` 和 `metafile_converter` 只在显式编译 DOCX 素材时动态载入；
- 路径 A 的已有 DOCX 格式化仍走生产管线，不依赖通用素材 DOCX 导入器；
- 源码环境中的复杂 DOCX 导入 39 项回归保持通过；
- 核心包若没有安装该可选能力，会返回“当前版本未安装复杂 DOCX 素材导入能力”，不会静默失败。

该切片将主入口从 610 模块 / 216,729 行降至 607 模块 / 214,020 行。

### 2.10 已完成：完整 Assets 与主题编辑器退出核心一级导航

> 后续更正（2026-07-30）：本节关于 Assets 的产品决策已由《资料包一级入口恢复闭环》取代。资料包现为默认一级资源并保持按需加载；本节仅保留为历史执行记录。

修改：

- `src/ui/panel_specs.py`
- `src/ui/panel_registry.py`
- `src/ui/sidebar.py`
- `src/ui/main_window.py`
- `src/ui/adapters/workbench_issue_navigation.py`
- `src/ui/panels/workbench/panel_v2.py`

边界：

- 核心一级页面：工作台、方案配置、模板管理、AI 文档助手、偏好设置；
- 核心材料入口：工作台“资料包填充”、快速执行中的素材上下文、AI 附件角色；
- 可选源码页面：完整资料包工作台、主题编辑器；
- 素材/字段/题图修复链接在核心版内路由到“资料包填充”，不再跳到不存在的一级页面。

可逆性：

- `create_panel("assets", bridge)` 和 `create_panel("theme", bridge)` 仍可在源码环境动态创建；
- `MainWindow(include_optional_panels=True)` 可加载完整可选页面；
- 默认 `MainWindow()` 不展示、不预载，也不形成 PyInstaller 静态依赖。

该切片将主入口从 607 模块 / 214,020 行降至 528 模块 / 189,637 行；Assets UI 可达模块为 0，主题编辑器不可达，AI/生产必需的素材内容模块仍保留 10 个。

### 2.11 已完成：完整批量工作台退出默认产品

边界：

- 默认 `MainWindow()` 使用不含批量生成卡片的 `CoreWorkbenchPanel`；
- `MainWindow(include_optional_panels=True)` 和直接创建 `WorkbenchPanel` 仍恢复完整批量 UI、批次重试与批量执行；
- 批量详情、批量来源区、批量报告、批次历史和公文批次 payload 改为源码完整版按需加载；
- 单文档素材生产仍保留 `image_transform_batch`，因为它负责同一文档内多图片的并行变换，不等于“多文档批量产品”；
- `material_batch` 暂时保留，因为 Bridge 状态和公文资料包仍复用其选择与路径校验契约。

结果：

- 主入口从 528 模块 / 189,637 行降至 522 模块 / 186,587 行；
- 5 个明确的完整批量模块从默认产品静态图退出；
- 源码完整版的批量详情 11 项、批量运行时 17 项、执行会话 36 项保持通过；
- 资源隔离 81 项通过，1 项仅因当前 Windows 用户无目录符号链接权限而跳过。

### 2.12 已完成：用户报告与内部证据写入器分离

处理：

- 保留 `src/report_writer.py` 作为源码工程环境的完整报告写入器；
- 新增 `src/product_report_writer.py`，只包含正式用户报告实际需要的变更、诊断、计数、交付、素材与旅程投影；
- 生产运行时只静态导入产品写入器；
- `include_internal_evidence=True` 时，源码环境仍动态委托给完整工程写入器；
- 增加产品/旧公共模式逐字节等价测试，防止“减行数”演变成报告字段缩水。

结果：

- 主入口有效行从 186,587 降至 185,089；
- 完整 `src.report_writer` 不再进入正式 PyInstaller TOC；
- 产品报告边界与等价性 7 项、报告诊断 36 项、工程样本报告 2 项、生产输出语义 46 项通过；
- 该切片净移出约 1,500 行只服务内部证据的静态产品代码。

### 2.13 已完成：场景产品投影与工程投影分离

处理：

- 新增 `src/ui/panels/scene_product_summary_projection.py`，只保留正式场景 UI 实际调用的常量和函数闭包；
- 默认产品页面全部改为导入产品投影；
- `src/ui/panels/scene_summary_projection.py` 继续保留完整矩阵、成熟度、样本、请求单元、参数归属和控件契约投影，供源码工程诊断使用；
- 产品入口门禁明确禁止完整工程投影进入静态图；
- 对 9 个内置场景、7 组常量、8 组场景函数和 3 组标量显示函数建立等价测试。

结果：

- 98 组内存比较零差异，覆盖空场景与实际样式投影调用形态的永久回归测试及产品边界合计 98 项通过；
- 产品投影为 1,044 有效行，完整工程投影为 3,050 有效行；
- 主入口从 185,089 降至 183,087 有效行，模块数保持 522；
- 实包净减少 2,002 有效行，完整工程投影未进入最终 TOC。

### 2.14 已完成：附件动态闭包精确收集

根因：

- 发布脚本原先使用 `--collect-submodules src.services.material_attachments`；
- 该规则额外收集 `timeline_preparation`；
- 动态收集的 `processing` 导入 `material_assets.image_document_executor` 时，会先执行过重的 `material_assets/__init__.py`；
- 旧包入口急切导入了题库、题图、修复审计和 Word 恢复，因此这些源码可选能力被连带装入默认产品。

处理：

- `src/services/material_assets/__init__.py` 改为 PEP 562 惰性公共导出，78 个既有公共名称和对象身份保持兼容；
- 发布脚本改为只显式收集 `src.services.material_attachments.processing`；
- 新增门禁，验证生产 processing 只加载图片计划、变换、验证和单文档执行闭包；
- 题库、题图、修复审计、Word 恢复和时间线准备继续可由源码 Assets 工作台按需加载。

结果：

- `timeline_preparation`、`question_figures`、`question_library`、`repair_audit`、`word_docx_recovery` 均未进入最终 TOC；
- 附件 processing、图片文档执行器和图片变换链仍在 TOC；
- 按当时 TOC 条目口径，实包从 187,695 降至 184,864，净减少 2,831 行；最终唯一源文件口径为 184,664 行；
- TOC-only 闭包从 4,608 降至 1,777 有效行；
- 附件生产与公共 API 87 passed、1 skipped，打包边界 19 passed，完整源码 Assets 可选模式 264 passed。

Word 真实预览 4 个模块、650 有效行继续保留。它直接服务考试卷和公文执行前预览，目前禁用分支只有错误提示而没有等价静态预览；在已经进入长期目标区间后，将其移出会构成功能降级。

### 2.15 最终真实规模

2026-07-29 重新执行完整发布脚本，结果：

| 指标 | 结果 |
|---|---:|
| 主入口可达模块 | 522 |
| 主入口可达有效行 | 183,088 |
| PyInstaller 第一方模块 | 559 |
| PyInstaller 第一方有效行（唯一源文件） | 184,664 |
| PyInstaller 第一方物理行（唯一源文件） | 209,864 |
| 发布目录文件数 | 552 |
| 发布目录总字节 | 133,210,224 |
| ZIP 字节 | 61,559,667 |
| EXE 字节 | 9,578,275 |

真实 TOC 中以下模块计数均为 0：

- 场景矩阵、发布治理、产品成熟度、样本覆盖、参数归属、控件契约；
- 完整 Assets、主题编辑器；
- 完整批量工作台、批量报告和批次历史；
- 复杂 DOCX 素材导入、旧内部报告写入器；
- 完整场景工程投影；
- 题库、题图、修复审计、Word 恢复和附件时间线工作台 handler。

最终 EXE 在 `QT_QPA_PLATFORM=offscreen` 下持续运行 8 秒，未提前退出；冒烟结束后只终止了本次启动的进程。

## 3. 功能验证

### 3.1 自动测试

本阶段已经通过：

| 测试范围 | 结果 |
|---|---:|
| 发布脚本测试 | 12 passed |
| UI 导出、主入口、Phase 0 smoke | 24 passed |
| 发布脚本 + 主入口复测 | 20 passed |
| 产品运行时边界、场景热导入和发布门禁 | 16 passed |
| 产品报告默认边界 | 1 passed |
| 内部产品成熟度报告显式模式 | 1 passed |
| 内部样本库报告显式模式 | 2 passed |
| AI 几何与结构预算定向测试 | 2 passed |
| AI 内容、能力、文档链与结构门禁组合 | 100 passed |
| 场景死 UI 移除与工程投影保留 | 16 passed |
| 生产结果不附加开发样本路径 | 6 passed |
| 用户/工程报告双模式 | 6 passed |
| 参数归属、控件契约、固定版式投影动态载入 | 18 passed |
| 最新产品运行时边界门禁 | 4 passed |
| 复杂 DOCX 素材导入源码能力 | 39 passed |
| AI Markdown 内容链 + 新边界 | 14 passed |
| 核心工作台导航与执行会话 | 66 passed |
| 核心窗口、可选面板与导入生命周期 | 21 passed |
| 完整可选 Assets 工作模式事务 | 24 passed |
| 默认/完整工作台边界 | 11 passed |
| 完整批量详情与运行时 | 28 passed |
| 执行会话架构 | 36 passed |
| 执行身份与资源隔离 | 81 passed, 1 skipped |
| 产品报告边界与逐字节等价 | 7 passed |
| 报告诊断完整回归 | 36 passed |
| 生产输出语义 | 46 passed |
| 场景产品投影等价 + 产品边界 | 98 passed |
| 场景 UI、身份、文案与布局直接回归 | 66 passed |
| 新场景投影架构断言 | 1 passed |
| 附件生产、公共 API 与惰性边界 | 87 passed, 1 skipped |
| 发布脚本、附件打包与产品边界 | 19 passed |
| 完整源码 Assets 可选模式 | 264 passed |

此外，实施前基线：

- 发布、主入口和 Phase 0：35 passed；
- AI 契约、计划、请求策略、生产适配、租约和恢复：29 passed；
- AI 内容链：86 passed，1 个结构预算失败；该失败已在本阶段修复。

### 3.2 真实 PyInstaller 构建

使用与发布脚本一致的：

- Qt hidden imports；
- 16 个共享 UI hidden imports；
- material attachments processing 精确收集；
- Qt 排除列表；
- defaults、config library、count profiles、icons、licenses 数据；
- 字体运行时 hook。

最终隔离构建结果：

- PyInstaller 6.21.0；
- 构建成功；
- 可执行文件成功生成；
- 发布目录 552 个文件，133,210,224 字节；
- 压缩包 61,559,667 字节，可执行文件 9,578,275 字节；
- 第一方唯一源文件 559 个，第一方有效代码 184,664 行；
- 16/16 共享 UI 白名单模块全部进入 Analysis TOC；
- 矩阵/发布治理、完整场景工程投影、完整 Assets/主题、完整批量、复杂 DOCX 导入、旧内部报告写入器和专用 Assets handler 均未进入 TOC；
- 使用 `QT_QPA_PLATFORM=offscreen` 启动后持续运行 8 秒，没有启动崩溃；
- 冒烟结束后只终止了该隔离测试进程。

PyInstaller 仍报告部分跨平台/可选依赖警告，以及将 `src.shared.ui` 导出名称误判为子模块的提示。底层 16 个真实模块均已存在，且实际启动通过；后续功能页巡检仍需覆盖延迟打开的面板。

## 4. 实施前治理问题的最终关闭

实施前遗留的工程治理红项已在产品边界稳定后继续修复，没有通过修改期望值、减少必需项或屏蔽报告来“刷绿”：

1. `AssistantCreativeHome` 方法预算已在前序阶段修复。
2. 材料修复流的 4 个失败来自代码拆分后的证据漂移：
   - 日志和结果状态证据迁移到 `quick_execution_feedback_mixin.py`；
   - 面板修复路由测试名更新为当前 `core_content_fill` 语义；
   - 单一材料 CTA 仍由真实按钮、信号和测试共同证明。
3. 控件契约摘要证据改指向真实实现 `scene_engineering_summary.py`，没有把工程注册表重新拉回产品闭包。
4. 对象预检证据改为验证当前统一状态入口 `_set_status(status_text, ...)`。
5. `LaTeX转Word完整工程` 与 PDF 论文路由的冲突通过给 PDF 论文路由增加 `LaTeX` 排除边界解决；预期仍保持 `matched -> import_ai_conversion_boundary`。

最终验证：

- 控件契约、材料修复、对象预检、任务词典报告均 `passed / 0 issue`；
- 请求单元审计 `0 issue`；
- 针对性审计与路由测试 23 passed；
- 发布验收 10/10 维度 ready；
- 矩阵仪表盘 `passed / 0 issue`；
- 矩阵钻取 37/37 ready、556/556 行、107/107 源证据 ready；
- 正式 scene matrix release gate 全部子检查 `passed / 0 issue`。

场景 UI 与矩阵组合测试仍是慢速门禁，后续可优化缓存和分层执行时间；这是测试效率问题，不再是正确性缺口。

## 5. 当前边界结论与下一步

矩阵外工程证据已完成解耦。最新主入口中名称含 `audit`、`registry`、`governance` 的剩余模块经逐个反向依赖审阅，均属于当前产品能力，而不是发布自证：

- AI 能力与工具注册表；
- 材料 schema、方案族、场景 surface 和生产模块注册表；
- 期刊规则来源校验；
- 图片修复结果审计；
- 一级面板注册表；
- 方案资料 token 的用户侧投影。

阶段 5 已完成通用 DOCX 导入、完整 Assets、主题编辑器、完整批量工作台和内部报告写入器的核心/可选边界；阶段 6 已完成场景产品投影与工程投影分离；阶段 7 已完成附件动态闭包精确收集。最新闭包快照：

| 候选能力 | 当前可达模块 | 当前有效行 | 边界判断 |
|---|---:|---:|---|
| 名称含 batch 的闭包 | 2 | 1,205 | 只剩共享选择/路径契约与单文档多图片变换；不能按文件名删除 |
| 预览/Office layout 闭包 | 30 | 11,429 | Office 图片落位 5,589 行属于生产正确性；Word/模板预览需另行拆分 |
| `services.material_content` | 10 | 4,600 | AI 路径 B 保留 Markdown 编译、组合、DOCX 渲染；复杂 DOCX 导入已可选 |
| Assets UI | 0 | 0 | 完整资料包工作台已移出核心入口，源码显式模式仍可加载 |

当前 184,664 行已经进入长期推荐区间，37/37 矩阵和请求单元治理债务也已关闭。下一步应降低巨型协调器的维护风险和慢速门禁成本，而不是继续移除产品能力。剩余 1,777 行 TOC-only 闭包中的附件 processing、图片文档执行、AI form tools 和共享 UI 白名单都有明确产品消费者；`office_layout_probe`、真实 Word 预览和 `image_transform_batch` 也分别服务生产排版、执行前预览和单文档多图片变换，当前均保留。
