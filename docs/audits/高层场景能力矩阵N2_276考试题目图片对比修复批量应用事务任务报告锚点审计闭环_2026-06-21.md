# 高层场景能力矩阵 N2.276 考试题目图片对比修复批量应用事务任务报告锚点审计闭环

日期：2026-06-21

## 1. 本轮定位

N2.276 承接 N2.275 的事务任务报告锚点动作桥。N2.275 已经能让 `question_figure_batch_apply_transaction_task_summary` action 打开 `report_path#question-figure-batch-apply-transaction-task-summary`，但高层 `scene_report_artifact_drilldown_audit` 仍只锁定 N2.180 的 9 条 report/artifact drilldown 通道，尚未把事务任务报告锚点作为独立审计通道纳管。

本轮只补“审计矩阵可见性”和“聚合视图一致性”：不新增后台任务中心，不执行回滚，不扩大事务执行语义。

## 2. 已闭合能力

- `scene_report_artifact_drilldown_audit` 新增 `question_figure_transaction_task_report_drilldown` 通道。
- 新通道覆盖 `question_figure_batch_apply_transaction_report` 与 `question_figure_batch_apply_transaction_task_summary` 两类 artifact。
- 审计证据连接 `question_figure_transaction_task_issue_items()`、Workbench action decode、RecentRunPanel artifact opener 和对应测试。
- RecentRunPanel 增加事务报告与事务任务锚点标签测试，锁定 `#question-figure-batch-apply-transaction-report` 与 `#question-figure-batch-apply-transaction-task-summary`。
- report/artifact drilldown 计数从 9/9 升级到 10/10，并同步到 release gate、dashboard 和 matrix drilldown 聚合测试。
- `scene_product_readiness` 在 `pack:exam_education` 与 `family:exam_teaching` 新增 `question asset comparison repair queue batch apply transaction task report-drilldown audit bridge` evidence。

## 3. 链路视角

```text
Workbench issue/action
  -> question_figure_batch_apply_transaction_task_summary
  -> report_path / artifact_path + task summary fragment
  -> RecentRunPanel._open_artifact_file(...)
  -> scene_report_artifact_drilldown_audit
  -> scene_matrix_dashboard / scene_matrix_drilldown / release gate
```

这条链路补上的不是新的执行能力，而是产品化审计视角：高层 readiness 说“任务报告锚点已打通”时，矩阵 drilldown 和 release gate 也能看到同一条通道，避免出现“实现有了、审计盲区仍在”的断层。

## 4. 新审计计数

- drilldown channel：10
- ready channel：10
- artifact kind：13
- runtime surface：24
- UI surface：18
- report surface：21
- repair target type：3
- test evidence：16
- source evidence：29
- missing source evidence：0

Dashboard / drilldown 聚合随之更新：

- scene matrix dashboard 中 `report_artifact_drilldown=10/10`。
- scene matrix drilldown 总行数从 394 更新为 395。
- `exam_education` 与 `batch_forms` 的 report/artifact drilldown 覆盖为 10。
- `quick_formatting` 的 report/artifact drilldown 覆盖为 9。

## 5. 仍不宣称完成的边界

- 不做完整批量事务中心。
- 不做后台任务中心。
- 不做回滚执行调度。
- 不做任务取消、重试或并发。
- 不做跨 docx 队列。
- 不做原子事务。
- 不做自动补偿。
- 不做文件快照系统。
- 不做远程素材库写回。
- 不做重复图片智能 disambiguation。
- 不做内容级错图识别。
- 不做复杂学科图或 AI 质量判断。

本轮只承诺：事务任务报告锚点已经进入 report/artifact drilldown 审计矩阵，并被 dashboard、matrix drilldown 与 release gate 共同锁定。

## 6. 修改范围

- `src/config/scene_report_artifact_drilldown_audit.py`
- `src/config/scene_product_readiness.py`
- `tests/test_recent_run_panel.py`
- `tests/test_scene_report_artifact_drilldown_audit.py`
- `tests/test_scene_product_maturity_upgrade_audit.py`
- `tests/test_scene_matrix_dashboard.py`
- `tests/test_scene_matrix_drilldown.py`

## 7. 验证

- 语法检查通过。
- 聚焦验证：`4 passed`。
- 聚合补丁验证：`16 passed`。
- 相邻回归：`37 passed`。
- 场景矩阵 release gate：passed，全部 `0 issues`。
- 全量回归：`1476 passed, 8 warnings`。

warnings 均来自 openpyxl 对 `datetime.utcnow()` 的弃用提示，不影响本轮事务任务报告锚点审计链路。
