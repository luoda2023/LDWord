# Alavette Form V1.0 发布前就绪审阅

> 文档状态：`current`
> 最终复审时间：2026-08-01 04:14（Asia/Shanghai）
> 审阅分支：`codex/code-structure-health-staging`
> 审阅基线 HEAD：`a0f8cf1060c1a8e900e875af85e12a6bab667270`
> 综合结论：**NO-GO——代码与自动化质量闸门已经通过，但当前工作区仍不是可签名、可追溯、可发布的正式候选。**

## 1. 最终结论

本轮已完成发布前代码、测试、场景能力、交付产物、安全、隐私、依赖、构建脚本和公开仓库治理复审，并直接修复了能够在当前工作区内关闭的问题。

当前状态应分成两层理解：

| 层级 | 状态 | 结论 |
| --- | --- | --- |
| 源码编译、关键架构与代码健康 | GO | 工程闸门通过 |
| 场景能力与发布证据矩阵 | GO | 54 项检查全部通过，0 issues |
| 全量自动化回归 | GO | 3078 通过、4 个环境性跳过、0 失败 |
| 发布依赖环境 | GO | Python/依赖与精确锁文件一致，`pip check` 通过 |
| 候选提交与可复现源码树 | NO-GO | 工作区存在大规模未提交、未跟踪变化 |
| Windows 正式签名构建 | NO-GO | 未配置签名证书，`signtool.exe` 不在 PATH |
| 最终二进制产物验收 | NO-GO | 尚未从干净提交生成并验证正式签名产物 |
| 人工兼容性矩阵 | NO-GO | Windows、Office/WPS、DPI、升级和受限权限矩阵尚未签核 |
| 真实发布远端与治理 | NO-GO | `origin` 仍是本地 bundle，尚无受保护远端/正式标签 |

因此：**当前代码质量可以进入“冻结候选”阶段，但不能发布现有 `dist/`、旧 ZIP 或任何未签名 QA 包。**

## 2. 本轮已经完成的改造

### 2.1 工程门禁与代码结构

- 修复 Ruff 阻断、失效导出和已经删除的 smoke 测试引用。
- 恢复 `scripts/engineering_gate.py`，使其真实执行 Ruff、编译、测试收集和关键烟雾回归。
- 完成 UI/运行时职责抽取，代码健康预算重新通过；没有通过简单上调预算掩盖问题。
- 将交付目标模板兼容性校验抽到共享、Qt-free 的服务层，执行会话和底层运行时使用同一条失败关闭规则。
- 将 `execution_runtime.py` 控制在架构预算内，保持只有一个运行器类及两个本地结果构造函数。
- 修复场景激活回滚、资料包关闭协议、UI 高度自适应和字体权重契约测试与当前真实实现的偏差。

### 2.2 场景、资料与交付产物链

- 恢复 V1 交付报告、失败报告、资料清单和资料包产物生成链。
- 增加产物发布事务与原子比较，避免部分成功或同名覆盖形成不一致结果。
- 补齐报告/产物下钻前端来源、资料修复入口、场景证据槽和验收证据。
- 清理已经删除的旧测试 owner 引用，把证据锚点迁移到仍存在且覆盖等价行为的测试。
- 将资料 schema 计数更新到当前真实注册表：23 个 schema，其中 22 个被场景引用。
- 对 V1 未实现的“交付预设使用另一套目标模板”明确失败关闭，不再静默按主模板输出。

### 2.3 发布 staging、扫描与构建链

- 新增 `scripts/stage_source_release.py`：只允许从干净 Git revision 导出源码，写入源码清单和 SHA-256，并支持清单复验。
- 扩展 `scripts/check_public_release.py`：区分源码树和最终二进制目录，校验必需文档、许可证、配置、SBOM、版本和文件哈希，并阻止日志、原始 Python 源、用户目录、本机路径及 DOCX 元数据/正文 PII 泄露。
- 新增 `scripts/build_release.py`：串联发布环境预检、工程闸门、场景闸门、全量回归、源码扫描、文档/许可证/配置生成、PyInstaller、版本资源、签名、SBOM、最终扫描、哈希和原子发布。
- 正式构建必须提供 40 位证书指纹并找到 `signtool.exe`；未签名产物只能使用 `--unsigned-qa`，且被明确标记为非正式发布。
- 正式与 QA 构建都必须从干净提交开始，不能直接打包当前开发目录。
- `scripts/windows/package_release.bat` 改为委托统一发布编排；清理脚本不再删除虚拟环境、用户数据或其他非构建目标。
- 受版本控制的 `Alavette-Form_V1.0.spec` 统一了 runtime hook、隐藏导入、发布文档、许可证和资源文件。
- 增加 Windows 版本资源生成器；应用语义版本与文件版本统一来自 `src/app_meta.py`。

### 2.4 依赖、CI 与供应链

- 新增 `requirements-release.lock`，锁定 Windows x64 / CPython 3.12 正式发布环境的直接与传递依赖。
- 新增 `scripts/verify_release_environment.py`，拒绝 Python 版本漂移、重复/无效锁条目、版本不一致和未锁定的已安装传递依赖。
- Windows 环境安装脚本固定 Python 3.12、pip 和发布锁文件，不再选择机器上的任意最高 Python。
- CI 工程矩阵覆盖 Python 3.10/3.11/3.12；正式全量和场景发布闸门固定 Python 3.12。
- GitHub Actions 第三方 action 已固定到完整 commit SHA。
- 增加 Dependabot 配置、变更日志、发布说明、安全策略、贡献指南、行为准则、Bug 模板和 PR 模板。
- 构建时基于实际运行时组件生成 SBOM/许可证清单，避免把 pytest、Ruff、PyInstaller 等构建工具误报为最终运行时组件。

### 2.5 用户数据、日志与 Provider 安全

- 运行时日志和可写配置/资源迁移到 `%LOCALAPPDATA%/Alavette-Form/`，并增加日志轮转。
- 内置资源保持只读，用户资源与程序目录分离，降低只读安装目录和覆盖升级风险。
- Provider 端点规则改为：远程地址必须 HTTPS；HTTP 仅允许本机回环地址；拒绝 URL 中的凭据、查询和 fragment。
- 阻止携带 `Authorization` 的跨 origin 重定向。
- 保留并回归凭据、正文、附件、历史和路径脱敏边界。

### 2.6 公开材料与隐私

- 清理公开文档中的真实本机用户路径，保留合成测试所需的明确例外。
- 检查全部 50 个已跟踪 DOCX 的本地路径、邮箱和核心作者元数据。
- 原 `tests/TEST-1/中图分类号.docx` 含真实姓名、电话、学号、邮箱和作者元数据，已从公开测试夹具位置移除；原件可恢复地保存在忽略目录 `exam_samples/private_pre_release_backup/2026-08-01/`，不得公开。
- 新增 `scripts/generate_public_docx_fixture.py`，用可复现的合成内容生成同名公开测试夹具，并增加结构、尺寸、表格和隐私回归测试。
- 合成 DOCX 已通过结构检查；本机缺少 LibreOffice `soffice`，Word COM 渲染尝试未正常结束，因此仍需在正式验收机补一次可视渲染检查。

## 3. 最终自动化验证证据

### 3.1 工程闸门

命令：

```powershell
python scripts/engineering_gate.py
```

结果：通过。

- Ruff 致命规则：通过；
- `compileall`：通过；
- 测试收集：3082 项；
- 关键烟雾测试：33 通过；
- 门禁总耗时：约 96 秒。

### 3.2 场景矩阵发布闸门

命令：

```powershell
python scripts/verify_scene_matrix_release_gate.py
```

结果：通过。

- 54 项发布检查全部 `passed`；
- 每项 0 issues；
- 12/12 高频场景包闭合；
- 37/37 drilldown 闭合；
- 556/556 drilldown rows 就绪；
- 107/107 来源证据就绪；
- 15/15 acceptance evidence 就绪；
- 10/10 requirement dimensions 就绪；
- 总耗时：约 236 秒。

### 3.3 全量回归

命令：

```powershell
python -m pytest -q tests
```

结果：

```text
3078 passed, 4 skipped in 4499.49s (1:14:59)
```

4 个跳过均为环境限制：

1. Windows 当前账户没有创建文件符号链接的权限；
2. Windows 当前账户没有创建目录符号链接的权限；
3. 附件资料接收测试所需符号链接不可用；
4. `QRawFont` 字形运行检查需要原生 Windows Qt 后端，当前自动化使用无头后端。

这些跳过没有造成当前测试失败，但正式发布前仍应在具备符号链接权限和原生 Windows UI 后端的专用任务中补跑或形成书面豁免。

### 3.4 发布环境

命令：

```powershell
.\.venv\Scripts\python.exe scripts/verify_release_environment.py
.\.venv\Scripts\python.exe -m pip check
```

结果：

- Python 与全部发布依赖精确匹配锁文件；
- 没有损坏的依赖关系。

### 3.5 构建失败关闭

使用发布虚拟环境验证：

- 正式构建：因缺少 `ALAVETTE_SIGN_CERT_SHA1` 而在构建前拒绝；
- 未签名 QA 构建：因 Git 工作区不干净而在源码导出前拒绝；
- 当前 PATH 中未找到 `signtool.exe`；
- 没有生成或覆盖可被误认为正式版的发行包。

这证明构建脚本不会把当前脏工作区或未签名产物发布为正式版本。

## 4. 当前剩余发布阻断（必须完成）

### P0-01 冻结并审阅唯一候选提交

当前 Git 状态：

| 项目 | 当前值 |
| --- | ---: |
| `git status --porcelain` 顶层条目 | 615 |
| 已跟踪变化 | 500 |
| 未跟踪条目 | 115 |
| 已暂存 | 0 |
| 已跟踪 diff | 500 个文件，新增 27,889 行，删除 116,771 行 |
| 构建器按单文件展开检测到的变化路径 | 722 |

必须执行：

1. 按功能域拆分审阅 500 个已跟踪变化和 115 个未跟踪条目；
2. 明确每个删除文件的迁移 owner 或删除裁决；
3. 确认所有正式源码、测试、默认资源、文档和发布脚本均被 Git 跟踪；
4. 建立候选 commit，并确保 `git status --porcelain` 为空；
5. 在干净 worktree/clone 中重新执行本文件第 3 节的全部命令。

验收：`stage_source_release.py` 能从该 commit 成功导出并复验源码清单。

### P0-02 配置真实发布远端、保护规则和标签

当前 `origin` 指向本地历史 bundle，不是实际发布托管远端；仓库也没有 `v1.0.0` 标签。

必须执行：

- 配置真实远端；
- 配置受保护分支和必需 CI 检查；
- 确认发布负责人和 CODEOWNERS；
- 为 `SECURITY.md` 配置可用的私密安全报告渠道；
- 全部验收完成后再创建不可变 `v1.0.0` 标签。

### P0-03 完成 Windows 代码签名能力

当前缺少签名证书配置，PATH 中没有 `signtool.exe`。

必须执行：

1. 在受控 Windows 构建机安装 Windows SDK/SignTool；
2. 配置组织代码签名证书和 `ALAVETTE_SIGN_CERT_SHA1`；
3. 确认 RFC 3161 时间戳服务可用；
4. 构建后执行 Authenticode `/pa /all` 验证；
5. 在标准用户 Windows 10/11 上检查 SmartScreen/杀毒软件行为。

验收：签名和时间戳验证通过，证书链可追溯，正式构建证据中保存验证日志。

### P0-04 从干净提交生成并验证正式产物

当前没有本轮代码对应的正式签名产物；已有 `dist/` 和旧 ZIP 均不得发布。

必须执行：

- 从 P0-01 的干净 commit 运行正式 `build_release.py`；
- 检查源码扫描和最终二进制扫描都为 0 errors/0 warnings；
- 验证 EXE 版本资源、签名、SBOM、许可证、文件清单、ZIP 和 SHA-256；
- 对解压后的最终包执行 GUI 启动、CLI、四个核心模式和文档生成 smoke；
- 将 commit、tag、工具链、清单、日志和哈希一起归档。

验收：最终产物与一个 commit/tag 一一对应，不能手工向旧目录补文件。

### P0-05 完成人工 Windows/Office 兼容性矩阵

至少完成：

| 维度 | 必测组合 |
| --- | --- |
| Windows | Windows 10 x64、Windows 11 x64、标准用户/非管理员 |
| 显示 | 100%、125%、150%、175%、200%，单屏/多屏 |
| Office | 无 Office、受支持 Microsoft Word、目标 WPS 版本 |
| 路径 | 中文、空格、长路径、OneDrive/同步目录、只读输入、不可写输出 |
| 文档 | 普通、修订/批注、域/目录、文本框、图片、超大、损坏和压缩炸弹样本 |
| 生命周期 | 首次启动、异常恢复、重复启动、覆盖升级、用户数据保留、回退和卸载 |
| 联网 | 离线、Provider 成功、超时、取消、TLS/证书失败、额度错误和跨域重定向 |

同时补做：

- 4 个环境性跳过项；
- 合成 DOCX 的 LibreOffice/Word 可视渲染；
- 标准用户下 `%LOCALAPPDATA%` 迁移、日志轮转和只读程序目录验证。

### P0-06 完成最终发布签核

必须由发布负责人对以下内容签核：

- release notes 与实际功能范围一致；
- V1 不支持的边界在 UI、文档和报告中一致显示；
- 隐私扫描没有真实用户材料、本机路径或 DOCX 作者信息；
- 第三方许可证与 SBOM 一致；
- 已知限制、升级/回退方式和安全报告入口可用；
- 正式产物下载地址、SHA-256 和签名验证说明已经准备好。

## 5. P1/P2 后续改进（不应阻塞候选冻结，但建议首发前规划）

### P1-01 为发布锁文件增加哈希

当前锁文件已经精确固定版本，但没有 wheel 哈希。建议使用受控方式生成带 `--hash` 的 Windows CPython 3.12 constraints，并验证离线/镜像安装，进一步降低依赖替换风险。

### P1-02 缩短全量回归反馈时间

本次全量测试耗时 1 小时 14 分 59 秒。建议：

- 将纯静态审计结果按源码 revision 缓存；
- 分离 PR 快速门禁、夜间全量和正式发布全量；
- 对互不共享全局状态的审计引入安全并行；
- 保存 `--durations` 报告并治理最慢测试。

目标：PR 必需反馈控制在 10～15 分钟，正式全量保持完整覆盖。

### P1-03 持续收紧异常捕获和诊断

现有代码仍有多处宽泛 `except Exception`。本轮没有把历史存量全部作为 P0，但应优先治理事务、持久化、发布构建、Office 自动化和用户可见失败链，确保异常既不静默丢失，也不会把敏感正文/路径写入日志。

### P1-04 完善公开仓库治理

本轮已补大部分治理文件，仍建议：

- 增加 CODEOWNERS；
- 在真实托管平台启用安全报告和依赖告警；
- 为版本标签、发布资产和回滚建立权限策略；
- 将历史审计标记为 `historical/superseded`，避免与当前结论冲突。

### P2-01 明确发行形态

当前交付形态是便携 ZIP。公开说明必须明确覆盖升级、用户数据目录、备份、回退和彻底卸载方式。若未来增加安装器，应单独验证安装器签名、升级代码和便携版数据兼容。

### P2-02 明确是否支持 PyPI

若只发布 Windows 桌面 ZIP，应明确“不提供 PyPI 包”。若要发布 PyPI，需另行补齐 wheel/sdist 元数据、非 Python 资源声明、入口点和干净环境安装测试，不能直接沿用桌面 ZIP 的通过结论。

## 6. 最终 GO 清单

### 已完成

- [x] 工程门禁返回 0；
- [x] 场景矩阵发布门禁返回 0；
- [x] 全量自动化回归 0 失败；
- [x] 代码健康预算通过；
- [x] 发布 Python/依赖锁验证通过；
- [x] `pip check` 通过；
- [x] Provider 远程 HTTPS、本地 HTTP 和跨 origin 授权重定向边界已加固；
- [x] 用户数据/日志与程序目录分离，日志轮转已实现；
- [x] 隔离 staging、源码/二进制扫描、SBOM、清单、哈希和签名门禁已实现；
- [x] 版本单一来源、版本资源和受控 PyInstaller spec 已实现；
- [x] 公开 DOCX 夹具已去真实个人信息并可复现生成；
- [x] 主要开源治理文档和模板已补齐。

### 未完成——全部关闭前保持 NO-GO

- [ ] 工作区变化已逐项审阅并形成干净候选 commit；
- [ ] 干净 clone/worktree 重新执行全部自动化闸门并导出源码清单；
- [ ] 4 个环境性跳过项已补跑或取得书面发布豁免；
- [ ] 真实远端、保护规则、CODEOWNERS 和安全报告渠道已启用；
- [ ] Windows 代码签名证书、SignTool 和时间戳服务已配置；
- [ ] 正式签名 EXE/ZIP 已从候选 commit 生成；
- [ ] 最终二进制扫描、签名、版本资源、SBOM、许可证和哈希全部通过；
- [ ] 最终包 GUI/CLI/核心业务 smoke 通过；
- [ ] Windows 10/11、DPI、Word/WPS/无 Office、标准用户矩阵已签核；
- [ ] 升级、备份、恢复、回退和卸载路径已验证；
- [ ] 合成 DOCX 已完成标准渲染器可视 QA；
- [ ] `v1.0.0` 标签和不可变发布资产已创建；
- [ ] release notes、已知限制、校验说明和发布负责人签核完成。

## 7. 发布负责人下一步

最短可靠路径：

1. 冻结当前开发写入；
2. 审阅并提交 615 个 Git 状态条目，建立干净候选 commit；
3. 配置真实远端、保护规则、SignTool 和签名证书；
4. 在干净 Windows 构建机运行正式发布脚本；
5. 执行最终包 smoke、人工兼容性矩阵和 DOCX 可视 QA；
6. 复核发布证据目录后创建 `v1.0.0` 标签和不可变发布资产。

在第 1～5 步全部完成前，发布状态保持 **NO-GO**。
