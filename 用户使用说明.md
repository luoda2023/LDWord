# LDWord 智能文档生成系统 — 用户使用说明

> 版本：当前构建版  
> 更新日期：2026-09-08

---

## 目录

1. [软件概述](#1-软件概述)
2. [快速开始](#2-快速开始)
3. [完整工作流程](#3-完整工作流程)
4. [核心功能详解](#4-核心功能详解)
5. [Markdown 编辑器操作](#5-markdown-编辑器操作)
6. [AI 辅助写作](#6-ai-辅助写作)
7. [导出 Word 文档](#7-导出-word-文档)
8. [偏好设置](#8-偏好设置)
9. [外部 AI 接口](#9-外部-ai-接口)
10. [常见问题](#10-常见问题)

---

## 1. 软件概述

LDWord 是一款 AI 驱动的智能文档生成系统。核心能力：

- **一句话生成文档**：告诉 AI 你要写什么，自动生成章节目录并逐章撰写正文
- **实时流式写作**：AI 写作时，文字、表格、标题实时显示在编辑器中
- **智能排版**：自动套用模板格式，标题编号、表格样式、页码页眉全部自动处理
- **超长文档支持**：支持 1000 页以上的文档，自动按卷拆分、断点续写
- **DOCX 导出**：一键导出为 Word 文件，含封面、目录、页码、分节

---

## 2. 快速开始

### 2.1 启动软件

双击 `LDWord.exe` 启动。首次启动时：

1. 软件会进入**助手对话界面**（左下角图标或自动打开）
2. 如未配置 AI 模型，请先在**偏好设置**中配置 API 密钥

### 2.2 配置 AI 模型

1. 点击左侧边栏的 **⚙ 偏好设置**
2. 找到 **AI 模型配置** 区域
3. 选择模型服务商（默认为 LUODA 官方服务）
4. 输入 API 密钥（从服务商处获取）
5. 点击**测试连接**确认配置正确

### 2.3 第一个文档（30 秒上手）

1. 在对话框输入：**"写一篇30页的工程项目可行性研究报告"**
2. AI 自动生成章节目录
3. 在目录确认卡上点击 **"确认目录，开始写正文"**
4. 观察 AI 逐章实时写入文字和表格
5. 写作完成后自动导出 Word 文件

---

## 3. 完整工作流程

### 3.1 第一步：提出需求

在对话框中输入你的文档需求。支持两种方式：

**方式一：自由对话（推荐）**

直接告诉 AI 你要写什么，例如：

```
写一篇50页的智慧园区建设项目可行性研究报告
```

```
帮我生成一份标书，项目名称是"XX市污水处理厂升级改造工程"
```

```
写一份2万字的技术方案，关于企业数字化转型
```

**方式二：导入参考文档**

1. 点击对话框下方的 **📎 附件按钮**
2. 选择一个参考 Word 文档（如模板、大纲、参考资料）
3. AI 会分析参考文档的结构和格式
4. 基于参考文档生成章节目录

### 3.2 第二步：生成章节目录

AI 收到你的需求后，会自动：

1. **分析文档类型**：判断是标书、报告、方案、论文等
2. **生成章节目录**：根据需求生成 5-20 个章节的目录结构
3. **显示在对话区**：目录以结构化卡片形式呈现

目录示例：

```
# 工程项目可行性研究报告

## 第一章 项目概述与可行性研究总论
  阐述项目背景、研究目的与范围...

## 第二章 市场分析与发展规划
  分析国内外市场需求与竞争格局...

## 第三章 技术方案与工程设计
  介绍项目建设方案与技术路线...

## 第四章 投资估算与经济效益分析
  编制项目总投资估算...

## 第五章 风险评估与结论建议
  识别项目风险并提出建议...
```

### 3.3 第三步：确认章节目录

**这一步必须由你确认**，AI 不会自动继续。

在章节目录确认卡上，你可以：

- ✅ **直接确认**：点击 **"确认目录，开始写正文"** 按钮
- ✏️ **修改目录**：点击 **"添加更多要求"** 告诉 AI 调整
  - 例如："把第三章拆成两章"、"增加一章关于环保的内容"
- 📋 **导入参考**：点击 **"导入参考项目文档"** 上传模板目录

确认后，章节目录会**自动进入左侧章节目录列表**，同时右侧 Markdown 编辑器自动打开。

### 3.4 第四步：逐章生成正文

确认目录后，AI 自动开始**逐章写作**：

- **左侧章节目录**：显示每章的写作状态（进行中/已完成/失败）
- **右侧 Markdown 编辑器**：实时显示 AI 写入的文字、表格、标题
- **对话区**：显示当前写作进度和状态

你可以：

- 👀 **观察写作过程**：文字像打字机一样逐字出现
- ⏹️ **点击停止**：随时点击右上角红色 **"停止生成"** 按钮中断
- 📊 **查看字数**：每章完成后显示字数统计

### 3.5 第五步：写作完成

所有章节写作完成后：

1. **自动排版复核**：系统自动检查标题编号、表格格式、页码一致性
2. **自动预检**：检查文档完整性、图片引用、格式合规
3. **自动导出 Word**：生成 `.docx` 文件并保存到指定位置

整个过程**只需一次点按确认**，后续全自动完成。

---

## 4. 核心功能详解

### 4.1 章节目录管理

**左侧章节目录列表**是文档的核心导航：

- 点击章节名 → 右侧编辑器跳转到对应章节
- 悬停章节 → 显示字数统计和状态
- 拖拽章节 → 调整章节顺序（如已确认）
- 右键章节 → 打开操作菜单（编辑/重写/删除）

### 4.2 系统记忆

AI 在写作过程中会自动维护**系统记忆文件**（`system_memory.json`），记录：

- 项目背景和整体方向
- 每章的摘要和关键信息
- 已使用的术语和风格

这确保 AI 在修改某一章时，不会忘记前面写过的内容。

### 4.3 超长文档支持

对于 100 页以上的超长文档：

- **自动按卷拆分**：每 100 页为一卷，每卷独立目录
- **断点续写**：中途关闭软件后重新打开，自动从上次中断处继续
- **检查点保存**：每章写完立即保存，不会丢失已完成内容
- **截断自动续写**：模型单次输出有长度上限，超限时自动续写拼接

### 4.4 模板系统

内置多种文档模板：

| 模板类型 | 适用场景 |
|----------|----------|
| 工程文档 | 可行性报告、技术方案、竣工验收 |
| 标书模板 | 投标文件、资格预审、商务标 |
| 公文模板 | 通知、报告、请示、批复 |
| 试卷模板 | 考试试卷、练习题、测评 |
| 论文模板 | 学术论文、毕业设计 |

模板定义了：
- 标题样式（字号、字体、编号格式）
- 正文格式（行距、段距、缩进）
- 表格样式（边框、对齐、表头格式）
- 页眉页脚（页码格式、公司名称）

---

## 5. Markdown 编辑器操作

右侧是 **Muya 编辑器**（基于 MarkText 的富文本编辑器），支持：

### 5.1 工具栏

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| **B** | 加粗 | Ctrl+B |
| *I* | 斜体 | Ctrl+I |
| ~~S~~ | 删除线 | — |
| U | 下划线 | Ctrl+U |
| H1-H6 | 标题 | Ctrl+1~6 |
| 📊 | 插入表格 | — |
| 📷 | 插入图片 | — |
| ƒx | 行内公式 | — |
| ƒx² | 块级公式 | — |
| 🔗 | 插入链接 | Ctrl+K |
| ⟲ ⟳ | 撤销/重做 | Ctrl+Z / Ctrl+Y |

### 5.2 右键菜单

在编辑器中选中文字后右键，可调用 AI：

- **AI 改写**：保持原意，换一种表达方式
- **AI 润色**：优化语言质量，提升专业性
- **AI 补充**：在光标位置自动添加相关内容

AI 执行时会显示阶段提示：
1. "了解章节上下文..."
2. "正在起草..."
3. "完成"

### 5.3 图片操作

- **粘贴图片**：Ctrl+V 粘贴剪贴板中的图片
- **拖拽图片**：直接拖入图片文件到编辑器
- **右键图片**：可编辑图注/替代文字
- 图片自动保存到项目目录并生成相对路径引用

### 5.4 表格操作

- **插入表格**：点击工具栏表格按钮，拖拽选择行列数
- **右键表格**：增删行列、合并单元格
- 表格逐行实时显示（AI 写作时）

### 5.5 自动保存

编辑器内容会自动保存到本地缓存：
- 切换章节时保存
- 导出时保存
- 关闭窗口时提示未保存改动

---

## 6. AI 辅助写作

### 6.1 浮动 AI 助手

打开 Markdown 编辑器后，右下角会出现 **AI 浮动图标**（🤖）：

- 点击图标 → 弹出悬浮对话框
- 在对话框中输入指令 → AI 会响应
- 对话框支持 Markdown 富文本显示（表格、标题、列表）
- 可拖动、可缩放、可收起

### 6.2 对话区交互

左侧对话区是主要的 AI 交互界面：

- **输入框**：输入你的需求或指令
- **附件按钮**：上传参考文档
- **发送按钮**：发送消息
- **停止按钮**（红色）：中断 AI 生成

### 6.3 AI 能理解的指令

AI 可以理解各种自然语言指令：

```
帮我把第三章的技术方案部分重写一下
```

```
在第五章增加一段关于风险控制的内容
```

```
把所有表格的列宽调整得更均匀
```

```
给第二章的市场分析部分补充一些数据
```

```
按照公文格式重新排版这份文档
```

---

## 7. 导出 Word 文档

### 7.1 自动导出

写作完成后，系统自动执行：

1. **排版复核校准**：统一标题编号、表格格式、页码
2. **预检**：检查文档完整性
3. **落盘**：生成 `.docx` 文件

### 7.2 手动导出

在章节目录列表或对话区，可以手动触发导出：

1. 点击 **"导出 Word"** 按钮
2. 选择保存位置
3. 确认导出信息（文件路径、预估页数）
4. 等待生成完成

### 7.3 导出设置

在导出确认框中，可以配置：

- **封面标题/副标题**：自动从项目名和文档类型带出
- **封面日期**：默认今天，可切换格式
- **图片质量**：原图/压缩/强压缩三档
- **模板选择**：从内置模板库选择排版模板

### 7.4 导出文件内容

生成的 Word 文件包含：

- **封面页**：标题、副标题、日期、公司名
- **目录页**：自动生成的章节目录
- **正文**：按模板排版的所有章节内容
- **表格**：按模板格式排版的数据表格
- **图片**：嵌入的图片和图注
- **页眉页脚**：页码、文档标题、公司信息
- **分节符**：每章独立分节，支持不同页眉

---

## 8. 偏好设置

### 8.1 AI 模型配置

| 设置项 | 说明 |
|--------|------|
| 模型服务商 | 选择 AI 模型提供商 |
| API 密钥 | 从服务商获取的密钥 |
| API 地址 | 自定义 API 端点 |
| 超时时间 | 单次请求最大等待时间 |

### 8.2 写作设置

| 设置项 | 说明 |
|--------|------|
| 流式速度 | 快/标准/慢三档，控制文字显示节奏 |
| 系统记忆位置 | 内部缓存目录或项目目录 |
| 撤销历史保留 | 导出后是否保留编辑历史 |

### 8.3 导出设置

| 设置项 | 说明 |
|--------|------|
| 封面日期格式 | 2026-09-08 / 二〇二六年九月 |
| 图片质量 | 原图/压缩/强压缩 |
| 导出后保留撤销 | 是否在导出后保留编辑历史 |

### 8.4 外部 AI 接口

| 设置项 | 说明 |
|--------|------|
| 启用外部 API | 默认关闭，开启后监听 127.0.0.1 |
| API 端口 | 默认 8765 |
| Token 认证 | 自动生成随机 Token |

---

## 9. 外部 AI 接口

开启外部 AI 接口后，外部 AI 可以通过 HTTP API 查询和控制本软件。

### 9.1 启用方式

1. 偏好设置 → 外部 AI 接口 → 勾选 **启用**
2. 记下生成的 Token（保存在用户目录）

### 9.2 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/api/capabilities` | 查询全部 AI 可用能力 |
| GET | `/api/state` | 当前工作模式和任务状态 |
| GET | `/api/sessions` | 会话列表 |
| GET | `/api/templates` | 可用模板列表 |
| GET | `/api/scenes` | 可用场景列表 |
| POST | `/api/chat` | 发送对话消息 |
| POST | `/api/action` | 执行控制动作 |

### 9.3 认证方式

所有请求需要带 Header：

```
Authorization: Bearer <your-token>
```

### 9.4 示例

```bash
# 健康检查
curl -H "Authorization: Bearer YOUR_TOKEN" http://127.0.0.1:8765/api/health

# 查询当前状态
curl -H "Authorization: Bearer YOUR_TOKEN" http://127.0.0.1:8765/api/state

# 发送对话
curl -X POST -H "Authorization: Bearer YOUR_TOKEN" -H "Content-Type: application/json" \
  -d '{"message": "帮我写一份技术方案"}' http://127.0.0.1:8765/api/chat
```

---

## 10. 常见问题

### Q: AI 不按我的要求写怎么办？

AI 会尽力理解你的意图，但有时需要更明确的指令。建议：

- 明确文档类型："这是一份**标书**，不是报告"
- 明确篇幅："写**30页**的文档"
- 明确结构："分成**5个主要章节**"
- 提供参考：上传模板文档让 AI 参照

### Q: 写作中途断了怎么办？

不会丢失已完成的内容：

1. 每章写完立即保存到本地缓存
2. 超长文档按卷自动创建检查点
3. 重新打开软件后，系统会检测未完成的任务
4. 选择继续即可从上次中断处恢复

### Q: 导出的 Word 格式不对怎么办？

导出前会自动运行排版复核校准。如果仍有问题：

1. 检查是否选择了正确的模板
2. 在 Markdown 编辑器中手动调整格式
3. 重新导出

### Q: 如何修改已生成的章节？

三种方式：

1. **在 Markdown 编辑器中直接修改**：点击章节 → 编辑内容 → 自动保存
2. **右键调用 AI 改写**：选中文字 → 右键 → AI 改写/润色
3. **在对话区让 AI 重写**：输入"重写第三章的技术方案部分"

### Q: 表格怎么显示不正常？

Markdown 表格需要正确格式：

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 数据1 | 数据2 | 数据3 |
```

确保分隔行（`|---|`）存在且列数一致。

### Q: 图片怎么插入？

三种方式：

1. **粘贴**：复制图片后 Ctrl+V
2. **拖拽**：直接拖入图片文件
3. **AI 生成**：让 AI 在内容中引用图片（需配置图片生成 API）

### Q: 1000 页的文档能写吗？

可以。系统支持：

- 自动按卷拆分（每卷约 100 页）
- 断点续写（中途关闭可恢复）
- 截断自动续写（超长章节自动拼接）
- 预计耗时：取决于 AI 模型速度，每章约 1-2 分钟

### Q: 如何使用自己的模板？

1. 准备一份格式规范的 `.docx` 文件
2. 在**模板管理**中导入
3. 在导出时选择该模板
4. 系统会自动套用模板格式

### Q: 软件支持哪些 AI 模型？

支持所有 OpenAI 兼容的 API，包括：

- LUODA 官方服务（内置）
- OpenAI GPT-4 / GPT-3.5
- Claude（通过兼容层）
- 国产大模型（通义千问、文心一言、DeepSeek 等）
- 本地部署模型（Ollama、vLLM 等）

---

## 附录 A：快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+B | 加粗 |
| Ctrl+I | 斜体 |
| Ctrl+U | 下划线 |
| Ctrl+Z | 撤销 |
| Ctrl+Y | 重做 |
| Ctrl+K | 插入链接 |
| Ctrl+V | 粘贴图片 |
| Enter | 发送消息（对话框） |
| Esc | 取消/关闭弹窗 |

## 附录 B：文件存储位置

| 内容 | 位置 |
|------|------|
| 应用配置 | `%APPDATA%\LDWord\` |
| 会话数据 | `%LOCALAPPDATA%\LDWord\assistant\sessions\` |
| 章节缓存 | `%LOCALAPPDATA%\LDWord\assistant\workbench\` |
| 系统记忆 | 会话目录内 `system_memory.json` |
| 外部 API Token | `%LOCALAPPDATA%\LDWord\assistant\external-api-token.json` |

## 附录 C：版本信息

- 当前版本：开发版（2026-09-08 构建）
- 核心组件：
  - Muya 编辑器（基于 MarkText）
  - PySide6（Qt for Python）
  - python-docx（Word 文件生成）
- AI 接口：OpenAI 兼容协议
- 支持平台：Windows 10/11（64位）

---

*本文档由 LDWord 系统自动生成，如有疑问请联系技术支持。*
