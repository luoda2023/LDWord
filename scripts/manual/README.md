# Manual Scripts

This directory contains interactive preview and debugging scripts. They are not
part of the automated pytest suite.

Run them from the project root, for example:

```powershell
python scripts/manual/test_workbench_v2.py
```
